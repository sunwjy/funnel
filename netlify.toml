# Netlify build config for the Funnel docs site (Astro Starlight).
# Base directory = repo root (default). Node version is read from .nvmrc at the root.
# Deploy previews are enabled automatically for pull requests.
[build]
  command = "pnpm install --frozen-lockfile && pnpm docs:build"
  publish = "docs/dist"
